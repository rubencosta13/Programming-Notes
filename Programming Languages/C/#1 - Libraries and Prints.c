#include <stdio.h> // Standard Input and Output Library (For Mouse / Keyboard (input devices) as well as monitors and printers(output devices))

int main(){
    printf("Hello World");          // This line of code will print on the VSC terminal "Hello World"
}
